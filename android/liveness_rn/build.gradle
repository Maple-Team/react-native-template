apply plugin: 'com.android.library'

android {
    compileSdkVersion 28

    defaultConfig {
        minSdkVersion 16
        targetSdkVersion 28

    }
}
repositories {
    flatDir {
        dirs project(':liveness').file('libs')
    }
}
dependencies {
    api "com.facebook.react:react-native:0.66.4"
    api project(":liveness")
}
